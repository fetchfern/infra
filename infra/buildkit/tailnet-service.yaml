apiVersion: v1
kind: Service
metadata:
  name: buildkit-tailnet
  annotations:
    tailscale.com/hostname: modrinth-buildkit
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: buildkit
  ports:
    - name: builtkit
      port: 1234
      targetPort: 1234