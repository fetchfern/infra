apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  selector:
    matchLabels:
      app: archon
      component: worker
  template:
    metadata:
      labels:
        app: archon
        component: worker
    spec:
      containers:
        args:
          ["/archon/archon", "--worker"]